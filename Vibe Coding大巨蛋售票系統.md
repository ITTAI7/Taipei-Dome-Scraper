# Vibe Coding 大巨蛋售票系統

> 本文件記錄「大巨蛋售票極速查詢系統」（Taipei Dome Ticket Scraper）的開發背景、歷程、技術架構與目前限制，作為後續提案規劃的參考資料。

---

## 一、專案緣起與目的

中華職棒（CPBL）多支球隊會在臺北大巨蛋舉辦主場賽事。賽事當天的**進場人潮多寡**直接影響：

- 需要調度多少「疏運人力／志工」到場支援動線引導
- 進場疏運需要抓多久的緩衝時間
- 各票區的售票熱度分布，用於研判人潮聚集熱點

但目前六支球隊使用**三套完全不同**的售票系統（宏碁資訊、新零售平台、ibon），且**沒有任何一套系統會主動提供「目前已售票數」的統計數字**——只能靠人工一個一個分區點進售票網頁，肉眼讀取「已售完 / 熱賣中 / 剩餘座位數」再手動加總，賽事前效率極低、也容易算錯。

**本專案的目標**：寫一支自動化查詢工具，一鍵抓取指定場次「各票區已售 / 未售票數」，並自動換算成疏運規劃需要的具體數字（已售人數、建議支援人力、疏運時間），供現場調度使用。

---

## 二、開發方式：Vibe Coding

本專案全程採用 **Vibe Coding**（人機協作、對話式 AI 輔助開發）的方式完成，開發者（規劃課同仁）**不需要手動撰寫爬蟲程式碼**，而是用自然語言描述需求、回報遇到的問題、確認技術方案，由 AI（Claude Code）直接讀取程式碼、動手實作、跑測試驗證、再回報結果，形成一個持續反覆的「描述需求 → AI 實作 → 一起驗證 → 修正」循環。

這個開發模式的特徵，具體展現在本專案的以下幾個真實案例中：

1. **從半成品到可用工具**：專案一開始只是零散的 `fetch-test*.ts` 測試腳本（見專案早期檔案），透過持續對話逐步收斂成一個有前端介面、有 API 層、有模組化爬蟲架構的完整系統。
2. **遇到問題即時診斷、不是憑感覺猜**：例如樂天/統一獅場次清單偶爾抓不到的問題，並不是靠「重開機試試看」解決，而是透過實際啟動本機伺服器、模擬併發請求、比對伺服器 log，逐步排除「Cloudflare 阻擋」「Chrome 版本不相容」「系統資源不足」等假設，最後鎖定是 **React StrictMode 導致同一支爬蟲被併發呼叫兩次、兩個瀏覽器搶同一份使用者資料而互相衝突**，並針對真正的成因（而非表面症狀）修復。
3. **每次修改都有實測佐證，不是「應該可以了」**：包含型別檢查（`tsc --noEmit`）、啟動本機伺服器實際打 API 驗證回應、甚至刻意模擬雙重請求測試併發修復是否生效。
4. **技術決策留有紀錄可追溯**：詳見 `DEVLOG.md`，記錄了「為什麼放棄 CloakBrowser 改用本機 Chrome」「為什麼半自動而非全自動繞過驗證」等關鍵決策的原因與踩坑過程，避免日後重蹈覆轍。

---

## 三、開發歷程（依時間順序）

| 階段 | 內容 |
|------|------|
| 初期 | 針對中信兄弟、富邦悍將、味全龍（皆使用「宏碁資訊」售票系統）建立 HTML 解析爬蟲；味全龍因售票系統需要登入，額外實作驗證碼登入流程 |
| 擴充 1 | 加入台鋼雄鷹：該隊使用完全不同的「新零售平台」JSON API，不能沿用前三隊的 HTML 解析邏輯，需獨立實作、並依球場（澄清湖／嘉義市／大巨蛋）動態帶入不同的全場預設總票數 |
| 擴充 2 | 加入樂天桃猿、統一獅：這兩隊的售票系統掛在 `ibon` 平台下，一開始用純 HTTP 請求會直接被 Cloudflare 判定為機器人攔下（回傳「驗證您是否是人類」challenge 頁），純解析 HTML 的既有作法完全失效 |
| 技術轉折 | 嘗試用 `cloakbrowser` 套件做無頭瀏覽器偽裝，結果落入 Cloudflare 驗證的無限迴圈；最終改採**半自動化**方案：用 `playwright-core` 直接操控使用者**本機真實安裝的 Chrome**，遇到驗證關卡時由人工手動點擊一次，程式偵測到通過後自動接手後續抓取，並把已驗證的 session 快取在本機 Chrome 設定檔中，減少重複驗證次數 |
| 擴充 3 | 加入職棒明星賽（All-Star Game），沿用樂天/統一獅已驗證可行的 ibon 瀏覽器繞過架構，共用同一套 `IbonBrowser.ts` 共用模組 |
| 穩定性強化 | 發現「同一支球隊的場次清單偶爾抓不到」的間歇性問題，追查後發現是前端 React StrictMode 造成同一請求被併發觸發兩次、搶用同一份瀏覽器設定檔導致衝突；修復方式為新增請求去重機制（同一隊已有進行中的請求時，後續請求共用同一個結果，不重複啟動瀏覽器），並將「無視窗優先嘗試、失敗才切換有視窗人工驗證」的邏輯補上失敗重試，降低偶發性錯誤 |
| 雲端佈署評估 | 為了讓「不需要靠本機瀏覽器繞過驗證」的四支球隊（中信、味全龍、富邦、台鋼）能提供給更多人使用，規劃將此系統佈署到 Google AI Studio 的雲端環境；同時盤點清楚**哪些球隊在雲端環境技術上就是無法運作**（因為雲端容器沒有真實瀏覽器、也沒有人可以手動點驗證），並在介面上明確反灰標示、加上提示文字，避免使用者誤以為系統故障 |

---

## 四、技術架構

```
瀏覽器（React 前端 SPA）
   │  一般 fetch API／EventSource（SSE 即時進度）
   ▼
Express 後端伺服器（server.ts，單一 Node 進程）
   │  依 platform 參數
   ▼
ScraperFactory → 對應球隊的 Scraper（各自獨立實作）
   ├─ BrothersScraper / FubonScraper / WeiChuanScraper   → cheerio 解析 HTML（宏碁資訊系統）
   ├─ TsgScraper                                          → 直接呼叫新零售平台 JSON API
   └─ RakutenScraper / UniScraper / AllStarScraper        → 純 HTTP 優先，被擋才用本機 Chrome
                                                              （共用 IbonBrowser.ts 瀏覽器控制模組）
```

**主要技術選型**：
- 前端：React 19、TypeScript、Vite、Tailwind CSS
- 後端：Express、tsx（直接執行 TypeScript，無需額外編譯步驟）
- 網頁解析：Cheerio
- 瀏覽器自動化：`playwright-core` 驅動本機真實 Chrome（而非虛擬/偽裝瀏覽器），提高被目標網站信任的機率

**模組化設計原則**（詳見 `AGENTS.md`）：即使多支球隊共用同一套售票系統，也刻意不共用邏輯——因為各隊的防爬蟲機制、頁面結構、特殊票區規則可能不同，修 A 隊的問題不能假設 B 隊也適用同樣修法，避免互相干擾。

---

## 五、核心功能

| 功能 | 說明 |
|------|------|
| 六隊 + 明星賽場次查詢 | 中信兄弟、味全龍、富邦悍將、台鋼雄鷹、樂天桃猿、統一獅、職棒明星賽，共七個查詢分頁 |
| 即時票務資料收集 | 各票區「未售出／已售出」張數，含「熱賣中」動態座位圖區塊的精確解析 |
| 即時進度回報 | 透過 SSE（Server-Sent Events），查詢過程中即時把「正在讀取哪個分區」等訊息推播到前端，避免使用者以為當機 |
| 支援人力自動試算 | 依「已售出人數」對照可自訂的門檻規則表，自動算出建議派遣的疏運人力人數 |
| 疏運時間試算 | 依已售人數換算預估疏運所需時間（分鐘） |
| 大巨蛋座位容量自動補齊 | 樂天／統一／明星賽內建 `大巨蛋座位.json` 各票區座位總數資料，遇到「熱賣中」等無法直接讀出總容量的區塊時，自動用「同分區已知總容量 − 已知已售/未售」反推補齊；台鋼雄鷹則是直接向官方 `activity-venues` API 取得每分區真實座位容量，不需要回推 |
| CSV 匯出 | 一鍵匯出各區售票明細，供後續留存或內部傳閱 |
| 味全龍登入驗證碼流程 | 味全龍售票系統需登入才能查詢，內建驗證碼辨識登入介面與 session 保存 |

---

## 六、開發過程中遇到的主要挑戰

1. **同一套售票系統，各隊仍有差異**：中信兄弟、富邦悍將、味全龍雖然用同一套「宏碁資訊」系統，但防爬蟲規則、頁面結構、特殊票區（輪椅席、活動席）的處理仍有差異，不能一套邏輯打天下。
2. **反爬蟲機制的攻防**：ibon 平台（樂天桃猿、統一獅、職棒明星賽三隊售票系統掛在此平台下）有 Cloudflare 等級的機器人防護，多次嘗試純程式化繞過都失敗，最終確認**必須有真實使用者透過本機瀏覽器手動驗證一次**才能取得信任，這也是為什麼這三隊目前**只能在本機（且需搭配行動網路，因固定 IP 較容易被判定為可疑來源）執行**，無法完全雲端化的根本原因。
3. **看似穩定、實則有隱藏 race condition 的 bug**：前端框架的開發模式特性（React StrictMode）會讓同一個初始化流程被觸發兩次，若沒有妥善處理併發，會讓兩個自動化瀏覽器進程搶用同一份資料而雙雙失敗——這類問題不容易從表面現象直接看出，需要實際模擬情境、比對日誌才能定位。
4. **雲端與本機的能力落差需要在產品層面誠實呈現給使用者**：與其讓使用者誤以為「系統壞了」，選擇在介面上明確標示「這幾隊雲端版暫不支援，需洽詢/使用本機版」，並附上聯絡窗口（規劃課分機8699）。

---

## 七、目前限制

- **樂天桃猿、統一獅、職棒明星賽**三隊，因售票系統的反爬蟲機制，**目前僅能在本機（且建議搭配行動網路熱點）執行查詢**，無法佈署到雲端供多人直接使用。
- 若要佈署到雲端（如 Google AI Studio）供中信兄弟、味全龍、富邦悍將、台鋼雄鷹四隊查詢使用，免費方案（Starter Tier）**僅限尚未曾建立過 Google Cloud billing 帳號的全新帳號**才能使用，且有部署數量（2 個應用程式內）與網域（僅能用 `ai.studio` 子網域）的限制。

---

## 八、可延伸討論的方向（供提案參考）

- 是否需要為「本機 + 行動網路」的查詢方式建立一套標準作業程序（SOP），讓非開發人員也能在賽事日自行操作三隊的查詢
- 是否評估與 ibon 官方接洽，取得正式的資料查詢授權或 API 存取管道，徹底解決反爬蟲限制
- 雲端版（四隊）是否要正式佈署供更多使用者查詢，若是，需要哪個 Google 帳號、由誰負責維護
- 本專案的「Vibe Coding」開發模式（AI 協作、快速迭代、每個修改都有實測驗證）是否可作為其他內部小工具開發的參考範本
